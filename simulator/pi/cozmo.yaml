procs:
  cozmo:
    shell: "sleep 2 && COZMO=1 bash mprocs/run_chroot.sh /vicBodyBridge"
  robot:
    shell: "sleep 2 && COZMO=1 bash mprocs/run_chroot.sh /anki/bin/vic-robot"
  engine:
    shell: "sleep 3 && COZMO=1 bash mprocs/run_chroot.sh /anki/bin/vic-engine"
  anim:
    shell: "sleep 4 && COZMO=1 bash mprocs/run_chroot.sh /anki/bin/vic-anim"
  cloud:
    shell: "sleep 5 && COZMO=1 bash mprocs/run_chroot.sh /anki/bin/vic-cloud"
